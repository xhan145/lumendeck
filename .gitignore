node_modules/
dist/
src-tauri/target/
*.local
__pycache__/
.venv/
/.cache/
/.lumendeck/
/diffusers-runtime/
/models/
/gallery/
/renders/
/outputs/
*.safetensors
*.ckpt
*.pt
*.pth
*.onnx
*.bin
*.gguf
*.mp4
*.mov
*.avi
*.mkv
*.webm
*.gif

# Updater signing keys + secrets — NEVER commit. The minisign PRIVATE key lives
# outside the repo at ~/.tauri/lumendeck-updater.key (see docs/RELEASING.md).
*.key
*.key.pub
.tauri/
.env
.env.*
!.env.example

# Bundled python-build-standalone runtime (fetched at build time, not committed)
src-tauri/resources/
